201022

出勤者の表示。配車画面上部。
最初 予定者情報を薄い字(シュシュ、カシュカシュわけ)
出勤時　色変える。シュシュ、カシュカシュ色。出勤時間を実時間に書き換える
退勤時  グレー色？

v-bind:class=
で色をかえてみる。

:classかいたが、データ変更されてない。
staffList内のデータを変更する。

着色できた。
起動時、登録済みのデータチェック。
初回のみだが、アプリ起動時にすでに登録された勤怠データがある場合、表示をフィットさせる。
staffListのstaffList状態を合わせる。

editorは別アプリにするか。
リストのリーダー。ダウンロードできるか？

