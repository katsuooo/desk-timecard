201110

qr あすから起動。
日付変わった時のデータの読み直しは？

タイマで１分置き？
appDateとnowの日付を比較。
ずれていると、nowでappDateを更新し、再描画する。

mainpanelでやるか、別モジュールにするか。
別でかいて、v-on:, $emit で通知する。



//-------
テスト動作時。

スクリーンセーバの起動で画面OFF
キー操作で画面をだすと、山下可奈子退勤となる。
最後のチェック者か。

名前検出後に、キーバッファをクリアしないとか？
できてないのか？やってそうだが。
クリアしてみた。再実験。

