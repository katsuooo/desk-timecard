201103

serverにdeploy
ローカルと動作がちがう。

ローカルでも、未登録者の勤退データが表示されない。
出勤時のスキャン時には、モニタ表示がでるが、退勤時には退勤時間が反映されない。
リロード時にも、退勤時間が表示されない。

日付頭から起動しているので、頻度は少ないが、途中起動時に当日登録済みデータが表示されないのは
不便。

日付変更時のリロードもまだ。


//----
staff sche リード時に初期データ生成している。
ここで、attendanceデータと合わせて、初期データを生成するよう、変更したい。

slist.registerOn
一人追加の関数。初期時は登録済みデータを一人づつこの関数におくる。



